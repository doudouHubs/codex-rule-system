interface:
  display_name: "Project Rule List"
  short_description: "List or search project-shared rules."
  default_prompt: "Use $project-rule-list to inspect active or deprecated project-shared rules by tag, keyword, or full list."

policy:
  allow_implicit_invocation: false
