interface:
  display_name: "Rule Project Pick"
  short_description: "Pick project rules into this session."
  default_prompt: "Use $rule-project-pick to copy selected active project-shared rules into the current session rule context by id, tag, or keyword."

policy:
  allow_implicit_invocation: false
