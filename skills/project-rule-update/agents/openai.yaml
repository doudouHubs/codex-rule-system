interface:
  display_name: "Project Rule Update"
  short_description: "Update a project-shared rule by id."
  default_prompt: "Use $project-rule-update to modify a project-shared rule's title, content, tags, or status by id."

policy:
  allow_implicit_invocation: false
