interface:
  display_name: "Rule Project Delete"
  short_description: "Deprecate or hard-delete a project rule."
  default_prompt: "Use $rule-project-delete to deprecate a project-shared rule by id, or hard-delete only when explicitly requested."

policy:
  allow_implicit_invocation: false
