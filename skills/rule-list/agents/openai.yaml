interface:
  display_name: "Rule List"
  short_description: "List rules from the current project session store."
  default_prompt: "Use $rule-list to inspect the current project's current-session rules as a full list or summary."

policy:
  allow_implicit_invocation: false
