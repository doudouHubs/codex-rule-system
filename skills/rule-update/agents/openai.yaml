interface:
  display_name: "Rule Update"
  short_description: "Update a rule inside the current project session store."
  default_prompt: "Use $rule-update to modify one existing rule in the current project's current-session rule store."

policy:
  allow_implicit_invocation: false
